// Prototype mock data — delete when real API is wired.
// Mirrors frontend/src/mockData/mockUserProfile.ts.
import '../models/user.dart';

const mockUserProfile = User(
  id: 'demo-user',
  identifier: '9876543210',
  name: null,
  city: null,
  segment: null,
  onboardingComplete: false,
);
